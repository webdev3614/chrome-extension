import { UIType } from "@/components/core/ui-render/types";

export const UI = [
    {
        name:"chain_id",
        type:UIType.SELECT,
        options:[]
    },
    {
        name:"wallet_index",
        type:UIType.SELECT,
        options:[]
    }
]